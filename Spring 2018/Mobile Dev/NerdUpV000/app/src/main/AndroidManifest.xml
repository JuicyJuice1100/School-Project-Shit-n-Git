<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.juice.nerdupv000">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:imeOptions="flagNoExtractUi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateLogin" />
        <activity android:name=".Profile" />
        <activity android:name=".EditProfile" />
        <activity android:name=".NewUser" />
        <activity android:name=".EditProfileGoogle" />
        <activity android:name=".Settings" />
        <activity
            android:name=".PasswordReset"
            android:label="@string/title_activity_password_reset"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".SearchUserProfile">

            <!--
            <intent-filter>
                    <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                        android:resource="activity_search_user_profile"/>
            -->
        </activity>
        <activity android:name=".EditNotes"></activity>
    </application>

</manifest>